Place your splash logo image here with the filename `splash_logo.png`.

Recommended size: 512x512 or adjust as needed for your design.

When you add the PNG, run:

flutter pub get

Then rebuild the app so the asset is included.